# Git hooks and shell scripts must use LF line endings so they run on macOS/Linux
# (a CRLF shebang breaks under /bin/sh). This overrides any local autocrlf setting.
.githooks/** text eol=lf
*.sh text eol=lf
